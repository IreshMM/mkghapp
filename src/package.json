{
  "name": "mkghapp",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --exec 'node --env-file .env server.js' --watch '*' --ext '*' --ignore .devcontainer --ignore node_modules",
    "prod": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
