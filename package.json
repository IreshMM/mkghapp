{
  "name": "github-app-manifest-docker-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "devStart": "nodemon --exec 'node --env-file .env server.js' --watch '*' --ext '*' --ignore .devcontainer --ignore node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
